# 代码修改优化文档

本文档记录对原始 YOLOv5-FasterNet 项目所做的全部兼容性修复和优化。项目原始代码基于 YOLOv5 v6.0（2021 年），在新版 Python 环境（Python 3.9 + PyTorch 2.7 + CUDA 12.8）下存在多处兼容性问题，以下逐项说明。

---

## 一、致命错误修复（导致程序无法运行）

### 1. `torch.load()` 加载权重失败

**问题现象：**

运行 detect.py / val.py / export.py / train.py 等所有加载 `.pt` 权重的脚本时报错：

```
TypeError: Incorrect type used for checkpoint: expected Tensor, StackedTensor, or dict, but got <class 'NoneType'>
```

**根本原因：**

PyTorch 2.6 起，`torch.load()` 的 `weights_only` 参数默认值从 `False` 改为 `True`，只允许加载张量/字典等安全类型。YOLOv5 的 checkpoint 包含自定义类实例（如 `Model`、`Detect`），被反序列化安全机制拦截。

**修改方案：**

在 `utils/general.py` 新增统一的兼容加载函数，所有 `torch.load()` 调用统一替换：

```python
def torch_load(file, map_location='cpu'):
    """兼容 PyTorch 2.6+ weights_only 默认值变更"""
    try:
        return torch.load(file, map_location=map_location, weights_only=False)
    except TypeError:
        return torch.load(file, map_location=map_location)  # 旧版 PyTorch
```

**涉及文件（共 10 个，16 处替换）：**

| 文件 | 替换处数 |
|------|----------|
| `utils/general.py` | 2（strip_optimizer、torch_load 定义） |
| `models/experimental.py` | 1（attempt_load） |
| `models/fasternet.py` | 6（独立定义 `_torch_load`，保持骨干文件可独立使用） |
| `train.py` | 2 |
| `conver_pt.py` | 2 |
| `hubconf.py` | 1 |
| `utils/loggers/__init__.py` | 1 |
| `utils/aws/resume.py` | 1 |

---

### 2. protobuf 与 tensorboard / onnx 不兼容

**问题现象：**

运行 train.py 或 export.py 时报错：

```
TypeError: Descriptors cannot be created directly.
If this call came from a _pb2.py file, your generated code is out of date and must be regenerated with protoc >= 3.19.0.
```

**根本原因：**

环境中 protobuf 6.x 与 tensorboard 2.10.1 / onnx 1.12.0 自带的旧版 `_pb2.py` 文件不兼容。protobuf 4.x+ 要求生成的代码必须用 protoc >= 3.19 重新生成，但这些库的旧版本没有更新。

**修改方案：**

在 `utils/general.py` 文件最顶部（所有 import 之前）设置环境变量，强制使用纯 Python 解析：

```python
import os as _os
_os.environ.setdefault('PROTOCOL_BUFFERS_PYTHON_IMPLEMENTATION', 'python')
```

同时在 `utils/loggers/__init__.py` 中用 try-except 包裹 `SummaryWriter` 导入，tensorboard 不可用时降级为空类，不影响训练主流程。

**涉及文件：**
- `utils/general.py`（环境变量，所有脚本共享）
- `utils/loggers/__init__.py`（SummaryWriter 降级处理）

---

### 3. `torch.cuda.amp` 被移除

**问题现象：**

```
ImportError: cannot import name 'amp' from 'torch.cuda'
```

**根本原因：**

PyTorch 2.7 移除了已弃用的 `torch.cuda.amp`，AMP 功能已迁移到 `torch.amp`。

**修改方案：**

在 `models/common.py` 创建兼容包装类：

```python
class _Amp:
    """兼容 torch.cuda.amp → torch.amp"""
    autocast = torch.amp.autocast
    GradScaler = lambda *a, **kw: torch.amp.GradScaler('cuda', *a, **kw)
```

在 `train.py` 和 `utils/autobatch.py` 中新增兼容函数，自动判断 PyTorch 版本选择正确 API：

```python
def amp_autocast(device):
    if check_version(torch.__version__, '2.0'):
        return torch.amp.autocast(device_type=device, enabled=...)
    else:
        return torch.cuda.amp.autocast(enabled=...)
```

**涉及文件：**
- `models/common.py`
- `train.py`
- `utils/autobatch.py`

---

### 4. Pillow 10.x 移除 `font.getsize()`

**问题现象：**

训练过程中绘制标签时崩溃：

```
AttributeError: 'FreeTypeFont' object has no attribute 'getsize'
```

**根本原因：**

Pillow 10.0 移除了已弃用的 `ImageFont.getsize()`，改用 `getbbox()`。

**修改方案：**

在 `utils/plots.py` 新增兼容函数：

```python
def font_getsize(font, text):
    """兼容 Pillow >=10.0（getsize 已移除，改用 getbbox）"""
    if hasattr(font, 'getsize'):
        return font.getsize(text)
    bbox = font.getbbox(text)
    return bbox[2] - bbox[0], bbox[3] - bbox[1]
```

替换 `utils/plots.py` 中 2 处 `self.font.getsize()` 调用。

**涉及文件：**
- `utils/plots.py`

---

### 5. 数据集下载 HTTP 308 重定向失败

**问题现象：**

训练时自动下载 COCO128 数据集报错：

```
urllib.error.HTTPError: HTTP Error 308: Permanent Redirect
```

**根本原因：**

`torch.hub.download_url_to_file()` 底层使用 `urllib`，不能正确处理 ultralytics 服务器返回的 308 永久重定向。

**修改方案：**

在 `utils/general.py` 新增 `download_url_to_file()` 函数，用 `requests` 库替代（自动跟随重定向），带 tqdm 进度条：

```python
def download_url_to_file(url, dst):
    """用 requests 替代 torch.hub.download_url_to_file，支持 308 重定向"""
    resp = requests.get(url, stream=True, allow_redirects=True)
    ...
```

替换 `check_dataset()` 中的下载调用。

**涉及文件：**
- `utils/general.py`

---

## 二、警告修复（不影响运行但产生噪音）

### 6. timm 导入路径弃用警告

**问题现象：**

```
FutureWarning: Importing from timm.models.layers is deprecated, please import via timm.layers
```

**根本原因：**

timm 0.9+ 将 `timm.models.layers` 移到了 `timm.layers`。

**修改方案：**

`models/fasternet.py` 中改为 try-except 兼容导入：

```python
try:
    from timm.layers import DropPath, to_2tuple, trunc_normal_
except ImportError:
    from timm.models.layers import DropPath, to_2tuple, trunc_normal_
```

**涉及文件：**
- `models/fasternet.py`

---

### 7. albumentations 2.x API 变更

**问题现象：**

```
UserWarning: 1 validation error for InitSchema
size: Field required

UserWarning: Argument(s) 'quality_lower' are not valid for transform ImageCompression
```

**根本原因：**

albumentations 2.x 中：
- `RandomResizedCrop` 的参数从 `height`/`width` 改为 `size`
- `ImageCompression` 的参数从 `quality_lower` 改为 `quality_range`

**修改方案：**

`utils/augmentations.py` 中用 `inspect.signature` 检测参数名，自动适配新旧版本：

```python
if 'size' in inspect.signature(A.RandomResizedCrop).parameters:
    crop = A.RandomResizedCrop(size=(size, size), ...)
else:
    crop = A.RandomResizedCrop(height=size, width=size, ...)
```

**涉及文件：**
- `utils/augmentations.py`（2 处）

---

## 三、功能优化

### 8. 阻止 check_requirements 自动降级 protobuf

**问题现象：**

train.py 启动时自动执行 `check_requirements()`，检测到 tensorboard 版本"不满足"后自动 pip 安装，将 protobuf 从 6.x 降级到 3.19.6，导致 wandb / visualdl 等其他包冲突。

**修改方案：**

`train.py` 中改为排除 tensorboard 和 thop 的自动检查（与 detect.py / val.py 保持一致）：

```python
check_requirements(exclude=('tensorboard', 'thop'))
```

**涉及文件：**
- `train.py`

---

## 四、代码清理

### 9. 删除无用/有 Bug 的文件

以下文件经评估存在 Bug 且无实际使用价值，已删除：

| 已删除文件 | 删除原因 |
|------------|----------|
| `base_ui.py` | PyQt5 图形界面，有 Bug 且功能与 detect.py 重复 |
| `ui_main.py` | UI 主窗口，依赖 base_ui |
| `ui_test.py` | UI 测试脚本 |
| `main.ui` | Qt Designer 界面文件 |
| `gradio_demo.py` | Gradio Web 演示，依赖缺失 |
| `yolov5_heatmap.py` | Grad-CAM 热力图，依赖 pytorch_grad_cam 且有多处 Bug |
| `yolov5.py` | 独立 ONNX Runtime 推理脚本，ONNX 输出解码逻辑错误导致黑屏 |
| `model/` 目录 | yolov5.py 的模型文件目录 |

---

## 五、修改汇总

| # | 问题 | 类型 | 涉及文件数 |
|---|------|------|-----------|
| 1 | torch.load weights_only | 致命 | 10 |
| 2 | protobuf 不兼容 | 致命 | 2 |
| 3 | torch.cuda.amp 移除 | 致命 | 3 |
| 4 | Pillow getsize 移除 | 致命 | 1 |
| 5 | HTTP 308 下载失败 | 致命 | 1 |
| 6 | timm 导入路径 | 警告 | 1 |
| 7 | albumentations API | 警告 | 1 |
| 8 | 自动降级 protobuf | 优化 | 1 |
| 9 | 删除无用文件 | 清理 | 8 个文件/目录 |

---

## 六、验证结果

所有核心功能在以下环境验证通过：

- Python 3.9.25
- PyTorch 2.7.0+cu128
- CUDA 12.8（RTX 5060 Laptop 8GB）
- protobuf 6.33.6 / Pillow 11.2.1 / timm 1.0.22 / albumentations 2.0.8

| 功能 | 命令 | 结果 |
|------|------|------|
| 推理 | `python detect.py --weights best.pt --source bus.jpg` | 3 persons, 42.5ms |
| 验证 | `python val.py --weights best.pt --data coco128.yaml` | mAP50=0.835 |
| 训练 | `python train.py --epochs 1` | 完整跑通，无报错 |
| 导出 | `python export.py --include onnx` | ONNX 13MB，2.3s |
| 压缩 | `python conver_pt.py --input best.pt` | 压缩成功 |
| Hub | `torch.hub.load(...)` | 加载成功 |
