# YOLOv5 推理项目修改优化文档 v2.0

> 基于原始 YOLOv5 7.0 裁剪项目，从单后端函数式脚本重构为四后端统一接口的推理工具包。

---

## 一、紧急 Bug 修复

| # | 问题 | 修复 |
|---|------|------|
| 1 | `weights/` 目录缺少 `yolov5s.pt`，程序直接崩溃 | 启动时检查权重文件是否存在，不存在给出明确提示 |
| 2 | `detect()` 每帧都调用 `model.warmup()`，推理速度减半 | warmup 移到模型加载时，只执行一次 |
| 3 | 18 个未使用的 import | 全部清理 |
| 4 | 无异常处理，报错直接抛堆栈 | 主入口加 try-except，输出友好错误信息 |
| 5 | 坐标转换冗余（xyxy→中心xywh→左上角xywh） | 直接从 xyxy 计算，减少中间步骤 |
| 6 | 缺少 `.gitignore` | 新建，忽略 `__pycache__/`、`.idea/`、`weights/*.pt` 等 |

---

## 二、兼容性修复

### 2.1 CUDA 设备选择

- **问题**：`torch.device("cuda")` 传给 YOLOv5 的 `select_device()` 不被识别，报 `Invalid CUDA '--device cuda'`
- **修复**：改用字符串 `'0'` 表示 GPU，`'cpu'` 表示 CPU；`loadmodel` 中兼容 `torch.device` 对象自动转换

### 2.2 PyTorch 2.6+ 权重加载

- **问题**：PyTorch 2.6 起 `torch.load` 默认 `weights_only=True`，加载 YOLOv5 旧权重报 `Unsupported global: models.yolo.Model`
- **修复**：在 `utils/general.py` 新增 `torch_load()` 函数，自动判断 PyTorch 版本，≥2.6 时加 `weights_only=False`，替换全部 5 处 `torch.load` 调用

### 2.3 屏蔽 YOLOv5 默认输出

- **问题**：每次加载模型都打印设备信息、Fusing layers、模型摘要
- **尝试**：环境变量 `YOLOV5_VERBOSE=False`（7.0 未使用）、`logging.disable(INFO)`（Windows 上无效）
- **最终方案**：`logging.disable(logging.WARNING)`，加载完成后恢复，作为 `verbose=False` 默认行为

---

## 三、架构重构

### 3.1 函数式 → 类封装

**`main_detect.py`** 从三个独立函数重构为 `YOLOv5Detector` 类：

```
之前: loadmodel() / detect() / detectdraw() 三个函数，状态靠参数传递
之后: YOLOv5Detector 类
      - __init__: 加载模型 + warmup（只一次）
      - detect(): 推理，支持 **kwargs 临时覆盖参数
      - draw(): 静态方法绘制检测框
      - __call__(): 支持 detector(img) 简写
      - 底部保留旧函数作为薄包装，向后兼容
```

### 3.2 提取公共基类

新增 `base_detector.py`，四个后端继承统一基类，消除重复代码：

| 公共方法 | 说明 |
|---------|------|
| `letterbox()` | 缩放填充，保持宽高比 |
| `scale_boxes()` | 检测框坐标映射回原图 |
| `nms()` | 纯 numpy 非极大值抑制 |
| `postprocess()` | 置信度过滤 + 类别选择 + NMS + 坐标转换 |
| `draw()` | 绘制检测框和标签 |
| `COCO_NAMES` | COCO 80 类名字典 |

重构后三个独立后端代码量减少约 60%：

| 文件 | 重构前 | 重构后 |
|------|--------|--------|
| `onnxruntime_detect.py` | 280 行 | 100 行 |
| `opencv_detect.py` | 250 行 | 80 行 |
| `tensorrt_detect.py` | 270 行 | 150 行 |

### 3.3 main.py 重构

- 全局宏定义式变量 → `argparse` 命令行参数（一行一个，YOLOv5 风格）
- 函数式流程 → `YOLOv5Inference` 类封装
- 新增 `--data` 参数支持自定义类别名（yaml 文件）

---

## 四、新增三个推理后端

### 4.1 ONNX Runtime（`onnxruntime_detect.py`）

- `YOLOv5OnnxRuntimeDetector` 类，接口与 PyTorch 后端完全一致
- 支持 `device='cuda'` / `'cpu'` / `'auto'` 切换
- **自动注入 PyTorch 的 CUDA DLL 路径**：onnxruntime-gpu 直接复用 torch 自带的 CUDA 12 + cuDNN 9，无需单独安装 CUDA Toolkit
- CUDA 首次推理自动 warmup

### 4.2 OpenCV DNN（`opencv_detect.py`）

- `YOLOv5OpenCVDNN` 类，仅依赖 OpenCV，无需 PyTorch/ONNX Runtime
- 检测 OpenCV 是否编译了 CUDA 后端，不支持时自动回退 CPU 并提示
- 使用简化后的 ONNX 模型（原始 ONNX 含 `Floor` 节点，OpenCV DNN 不支持）

### 4.3 TensorRT（`tensorrt_detect.py`）

- `YOLOv5TensorRTDetector` 类，FP16 推理
- 适配 TensorRT 10.x 新 API：
  - `max_workspace_size` → `set_memory_pool_limit()`
  - `execute_async_v2` → `execute_async_v3` + `set_tensor_address()`
  - `num_io_tensors` + `get_tensor_name()` 替代旧 binding API
- engine 加载失败时给出明确错误提示
- imgsz 与 engine 不匹配时提前报错
- `close()` 方法显式释放 GPU 内存，`__del__` 兜底
- 初始化时自动 warmup

### 4.4 后端切换

```bash
python main.py --backend pytorch       # PyTorch CUDA
python main.py --backend onnxruntime   # ONNX Runtime CUDA
python main.py --backend opencv        # OpenCV DNN CPU
python main.py --backend tensorrt      # TensorRT FP16
```

---

## 五、模型导出

| 脚本 | 功能 |
|------|------|
| `export.py` | YOLOv5 官方导出（保留），修复 protobuf 版本冲突（设环境变量 `PROTOCOL_BUFFERS_PYTHON_IMPLEMENTATION=python`） |
| `export_tensorrt.py` | 新增，ONNX → TensorRT engine，支持 `--weights`/`--output`/`--imgsz`/`--workspace`/`--no-fp16` 参数 |

> ONNX 模型需用 `onnx-simplifier` 简化后才能被 OpenCV DNN 加载。

---

## 六、其他优化

| # | 优化项 | 说明 |
|---|--------|------|
| 1 | imgsz 自动对齐 | 非 32 倍数时自动调整并打印警告（如 600→608） |
| 2 | 自定义类别名 | 所有后端支持 `names` 参数或 `--data` 指定 yaml，适配自训练模型 |
| 3 | draw 标签贴顶 | 目标在图片顶部时标签自动画到框内，不会画出去 |
| 4 | `--view-img` 修复 | 改用 `BooleanOptionalAction`，支持 `--no-view-img` 关闭弹窗 |
| 5 | 文件精简 | 从 100+ 文件精简到核心文件，删除训练/部署/日志等无关模块 |
| 6 | `benchmark.py` | 新增多后端速度基准测试脚本 |
| 7 | `requirements.txt` | 精简为推理所需依赖，标注可选依赖 |

---

## 七、性能对比（RTX 5060, 640×640, 100 帧平均）

| 后端 | 速度 | FPS |
|------|------|-----|
| TensorRT FP16 | 13.7 ms | 72.8 |
| PyTorch CUDA | 14.3 ms | 69.9 |
| ONNX Runtime CUDA | 14.6 ms | 68.4 |
| ONNX Runtime CPU | 74.1 ms | 13.5 |
| OpenCV DNN CPU | 562.4 ms | 1.8 |

四后端在 bus.jpg 上检测结果完全一致：4 person + 1 bus。

---

## 八、最终文件结构

```
yolov5_infer-master/
├── main.py                    # 主入口（YOLOv5Inference 类 + argparse）
├── base_detector.py           # 公共基类
├── main_detect.py             # PyTorch 后端
├── onnxruntime_detect.py      # ONNX Runtime 后端
├── opencv_detect.py           # OpenCV DNN 后端
├── tensorrt_detect.py         # TensorRT 后端
├── benchmark.py               # 速度基准测试
├── detect.py                  # YOLOv5 官方检测脚本（保留）
├── export.py                  # YOLOv5 官方导出（保留）
├── export_tensorrt.py         # TensorRT engine 导出
├── models/                    # YOLOv5 模型定义
├── utils/                     # YOLOv5 工具函数
├── data/coco128.yaml          # COCO 类别配置
├── weights/                   # 权重文件 (.pt / .onnx / .engine)
├── images/                    # 测试图片
└── requirements.txt
```
