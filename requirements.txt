# YOLOv5 多后端推理依赖
# Usage: pip install -r requirements.txt

# 核心（PyTorch 后端）
torch>=1.7.0
torchvision>=0.8.1
opencv-python>=4.1.1
numpy>=1.18.5
Pillow>=7.1.2
PyYAML>=5.3.1
requests>=2.23.0
tqdm>=4.64.0
scipy>=1.4.1
matplotlib>=3.2.2
psutil

# ONNX Runtime 后端（GPU 版，CPU 版用 pip install onnxruntime）
onnxruntime-gpu>=1.15.0

# OpenCV DNN 后端（无需额外依赖，opencv-python 已包含）

# TensorRT 后端（需单独安装，pip install tensorrt pycuda）
# tensorrt
# pycuda

# 模型导出（export.py / export_tensorrt.py）
onnx>=1.9.0
onnx-simplifier>=0.4.1
